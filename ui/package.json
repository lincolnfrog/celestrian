{
  "name": "celestrian-ui-tests",
  "version": "1.0.0",
  "description": "Celestrian UI integration tests",
  "type": "module",
  "scripts": {
    "test": "for f in js/tests/*.mjs; do echo \"--- $f\"; node \"$f\" || exit 1; done",
    "test:playwright": "npx playwright test",
    "test:all": "npm run test && npm run test:playwright"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "serve": "^14.2.5"
  },
  "license": "AGPL-3.0-only",
  "private": true
}
